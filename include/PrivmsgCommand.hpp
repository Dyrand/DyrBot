class PrivmsgCommand
{
	std::string command_name;
	//Standard permissions
	basic_permissions(
	//Any other permissions a command might want to implement
	custom_permissions(
	//Checks both basic and custom permissions
	check_permission()
}